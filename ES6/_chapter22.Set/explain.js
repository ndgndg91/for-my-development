// Set 오브젝트는 값을 배열로 작성한다.
// 추가한 순서대로 인덱스를 부여하여 순서가 보장된다.
// key 개념을 갖고 있으며, 값들이 값과 동시에 키 역할도 한다.
// key 개념은 중복을 방지하기 위해서 존재한다.
// 따라서 value값이 같으면 나중에 추가한 값으로 대체된다.
//
// value에는 string, number, symbol 프리미티브 타입과, Object, Function 오브젝트도 작성가능하다.
// null은 undefined로 취급된다.
